/** Inline "Hook" brand word in the Glue-logo lettering (pink fill, blue sticker stroke). */
export function Hookmark({ className = "" }: { className?: string }) {
  return <span className={`hookmark ${className}`}>Hook</span>;
}
